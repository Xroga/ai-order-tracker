{
  "name": "xroga-ai-order-tracking",
  "version": "1.0.0",
  "description": "Xroga AI Order Tracking for Shopify",
  "main": "functions/webhooks.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node test-webhook.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
